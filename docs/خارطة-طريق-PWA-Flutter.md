# خارطة طريق تحويل النظام إلى PWA ثم Flutter

## 1. القرار المعماري

الهدف المعتمد هو:

> نبني PWA لتحسين الاستخدام الحالي واختبار تجربة الهاتف، وفي الوقت نفسه نعيد تنظيم Laravel ليصبح Backend آمنًا ومشتركًا للويب وFlutter.

لا تعني هذه الخطة إعادة كتابة المشروع دفعة واحدة، ولا تسمح بإنشاء منطق مخزون أو محاسبة مستقل داخل المتصفح أو Flutter. يبقى Laravel المصدر الوحيد للحقيقة، وتستخدم واجهات Blade وPWA وFlutter خدمات العمل نفسها.

## 2. مبادئ لا تتغير أثناء التنفيذ

1. **Laravel هو صاحب القرار:** الصلاحيات، يوم العمل، المخزون، البيع، الجرد، النقل، الشفتات والحسابات تعتمد من الخادم فقط.
2. **لا نجاح وهمي:** لا تظهر رسالة نجاح لعملية حساسة قبل تأكيد الخادم لها.
3. **Offline للمسودات لا للاعتماد:** يسمح بكتابة مسودة محلية، ولا يسمح باعتماد بيع أو نقل أو جرد أو شفت دون اتصال.
4. **API بإصدار واضح:** تبدأ الواجهات الجديدة من `/api/v1` ولا ترتبط بتفاصيل Blade.
5. **خدمات مشتركة:** Web Controller وAPI Controller يستدعيان Application Service نفسها.
6. **التدرج:** لا تبدأ مرحلة قبل اجتياز بوابة قبول المرحلة السابقة.
7. **الأمان والعزل:** كل استجابة وكل Cache وكل إشعار مرتبط بالمستخدم والدور والمتجر المصرح به.
8. **قابلية القياس:** لا نعالج ضغط الخادم بالتخمين؛ نضع قياسات ثم نحسن أبطأ المسارات المثبتة.
9. **هوية موحدة:** الويب وPWA وFlutter يستهلكون رموز تصميم موحدة، مع منع الألوان العشوائية داخل الصفحات.
10. **الرجوع الآمن:** كل إطلاق كبير يملك Feature Flag وخطة تعطيل لا تمس البيانات.

## 3. النطاق المستهدف

### 3.1 PWA

- واجهة الويب الحالية بعد تحسينها للهاتف.
- تثبيت على الشاشة الرئيسية.
- Shell يعمل عند ضعف الاتصال.
- إشعارات وروابط عميقة.
- مسودات محلية محددة.
- مراقبة حالة الاتصال والمزامنة.

### 3.2 Flutter

- تطبيق مستقل للعمليات اليومية.
- يبدأ بالمحاسب والمهام التشغيلية.
- يتصل بـLaravel API فقط.
- يستخدم قاعدة محلية للمسودات والقراءة المؤقتة، لا كسجل مالي مستقل.

### 3.3 ما يبقى على الويب أولًا

- الإدارة العامة.
- إعدادات الاشتراكات والصلاحيات.
- التقارير الثقيلة.
- الاستيراد والتصدير.
- إعدادات النظام الحساسة.
- عمليات الصيانة والتدقيق.

## 4. مسارات العمل المتوازية

تقسم الخطة إلى ثمانية مسارات، ويملك كل مسار قائمة مهام وبوابة قبول:

1. تدقيق النظام وتجربة الهاتف.
2. إعادة تنظيم Laravel وAPI.
3. PWA والتخزين المؤقت.
4. Offline والمزامنة.
5. الإشعارات والروابط العميقة.
6. البحث والأداء وقاعدة البيانات.
7. نظام التصميم ومركز التحكم.
8. Flutter والإطلاق التشغيلي.

---

# المرحلة 0: الحوكمة وخط الأساس

## الهدف

منع التشتت قبل بدء التنفيذ، وتسجيل الوضع الحالي رقميًا.

## الأعمال

- إنشاء سجل قرارات معماري ADR لكل قرار كبير.
- تعيين مالك لكل مسار: Backend، PWA، Flutter، UX، أمن، تشغيل.
- حصر الأدوار: مالك، محاسب، مدير، كاشير، وأي أدوار مستقبلية.
- رسم أهم رحلات الاستخدام من البداية للنهاية.
- حصر الصفحات والعمليات الحالية وتصنيفها: قراءة، مسودة، اعتماد حساس.
- تسجيل خط أساس للأداء: زمن الاستجابة، عدد الاستعلامات، حجم الصفحة، أخطاء 5xx، واستهلاك الذاكرة.
- تعريف بيئات منفصلة: تطوير، اختبار، Staging، إنتاج.
- تعريف سياسة النسخ الاحتياطي والاسترجاع قبل أي تغيير بنيوي.

## المخرجات

- قائمة شاشات مع الأولوية.
- خريطة صلاحيات.
- قائمة العمليات الحساسة.
- لوحة قياس أولية للأداء والأخطاء.
- سجل مخاطر وخطة رجوع.

## بوابة القبول

لا تبدأ PWA قبل معرفة الصفحات ذات الأولوية والعمليات التي يمنع تشغيلها Offline.

---

# المرحلة 1: تدقيق وإصلاح الأساس الحالي

## 1.1 الإشعارات

تراجع دورة الإشعار كاملة:

- مصدر الحدث.
- المستلم الصحيح حسب الدور والمتجر.
- منع التكرار.
- تخزين الإشعار الداخلي.
- تسجيل الجهاز وتحديث رمزه.
- إلغاء الرمز عند الخروج أو إبطال الجلسة.
- حالة القراءة.
- الرابط العميق للصفحة المطلوبة.
- عدم وضع بيانات حساسة في نص Push.
- إعادة المحاولة عند فشل المزود.
- سجل تسليم وفشل قابل للتدقيق.

## 1.2 البحث

إنشاء عقد بحث موحد بدل الاستعلامات المتفرقة:

- تطبيع النص العربي والمسافات.
- دعم الاسم والباركود والمعرف عند الحاجة.
- Pagination إلزامية.
- حد أدنى لطول البحث في الاستعلامات الواسعة.
- Debounce في الواجهة.
- إلغاء الطلب السابق عند كتابة بحث جديد.
- ترتيب نتائج ثابت ومفهوم.
- فهارس قاعدة بيانات مبنية على الاستعلامات الحقيقية.
- قياس زمن البحث وحجم النتيجة.

يبدأ البحث بـSQL وفهارس صحيحة. لا يضاف محرك بحث خارجي إلا إذا أثبتت القياسات أن قاعدة البيانات لم تعد كافية.

## 1.3 سلامة الخدمات

- نقل قواعد العمل من Controllers الكبيرة إلى Services أو Actions قابلة لإعادة الاستخدام.
- توحيد التحقق والصلاحيات بين الويب وAPI.
- إضافة Transactions وقفل مناسب للعمليات الحساسة.
- إضافة Idempotency للطلبات التي قد يعيد الهاتف إرسالها.
- إضافة رقم نسخة أو تحقق من التعديل المتزامن عند الحاجة.
- توحيد أكواد الأخطاء التشغيلية، لا الاكتفاء برسائل نصية.

## 1.4 الاختبارات

- Feature Tests لرحلات البيع والشفت والنقل والجرد والإشعارات.
- اختبارات عزل المتاجر والصلاحيات.
- اختبارات منع تكرار الطلب.
- اختبارات التزامن على المخزون.
- اختبارات التاريخ التشغيلي.
- اختبارات ترحيل قاعدة البيانات والرجوع.

## بوابة القبول

- لا توجد عملية حساسة جديدة تعتمد على Controller فقط.
- العمليات عالية الخطورة مغطاة باختبارات Feature.
- يوجد تعريف واضح لأكواد الأخطاء التي سيستهلكها Flutter.

---

# المرحلة 2: تأسيس Backend مشترك وAPI v1

## 2.1 الهيكل

```text
Blade/Web Controller ─┐
                      ├── Application Service/Action ── Models/Database
API v1 Controller ────┘
```

## 2.2 نطاق API الأول

```text
/api/v1/auth/login
/api/v1/auth/logout
/api/v1/me
/api/v1/devices
/api/v1/app-config
/api/v1/dashboard
/api/v1/stores
/api/v1/products
/api/v1/search
/api/v1/notifications
/api/v1/store-transfers
/api/v1/inventory-counts
```

تضاف المبيعات والشفتات والتحصيلات بعد استقرار العقود الأولى.

## 2.3 قواعد الاستجابة

- Envelope موحد للنجاح والفشل.
- أكواد خطأ ثابتة مثل `INSUFFICIENT_STOCK`.
- Validation errors مرتبطة بالحقول.
- Pagination موحدة.
- تاريخ بصيغة ISO 8601 مع Timezone واضح.
- معرف طلب `request_id` للتتبع.
- عدم كشف Stack Trace أو أسماء الجداول.

## 2.4 المصادقة والأجهزة

- Tokens قابلة للإبطال.
- تسجيل كل جهاز باسم ومنصة وإصدار.
- حد لعدد الجلسات عند الحاجة.
- تسجيل خروج من جهاز أو جميع الأجهزة.
- Rate limiting حسب المستخدم والجهاز والمسار.
- صلاحيات منفصلة للمالك والمحاسب والمتجر.

## 2.5 التوافق

- إصدار API داخل الرابط.
- حد أدنى لإصدار التطبيق.
- Feature flags حسب الإصدار.
- سياسة إيقاف الإصدارات القديمة.
- Contract tests تمنع كسر الحقول دون قصد.

## بوابة القبول

- تطبيق عميل تجريبي يستطيع تسجيل الدخول وقراءة المنتجات والإشعارات عبر API.
- لا يوجد منطق مخزون منسوخ داخل API Controller.
- اختبارات تمنع الوصول إلى متجر غير مصرح به.

---

# المرحلة 3: بناء PWA الأساسية

## 3.1 قابلية التثبيت

- Web App Manifest عربي وRTL.
- اسم قصير واسم كامل.
- أيقونات معتمدة بكل الأحجام.
- `display: standalone`.
- `start_url` آمن.
- Theme وBackground من الهوية المعتمدة.
- اختصارات للعمليات الأكثر استخدامًا.

## 3.2 Service Worker

تعتمد استراتيجية Cache حسب نوع المورد:

| المورد | الاستراتيجية |
| --- | --- |
| CSS/JS والخطوط والأيقونات المرقمة | Cache First مع إصدار البناء |
| التنقل العام | Network First مع Offline fallback |
| بيانات غير حساسة قصيرة العمر | Stale While Revalidate عند الحاجة |
| العمليات المالية والمخزنية | Network Only |
| صفحات التقارير والبيانات الحساسة | عدم التخزين إلا بقرار صريح |

## 3.3 دورة التحديث

- اكتشاف نسخة Service Worker الجديدة.
- إظهار تنبيه «يتوفر تحديث» بدل استبدال الصفحة أثناء الإدخال.
- تطبيق التحديث بعد حفظ المسودة أو موافقة المستخدم.
- تنظيف Cache القديم.
- تسجيل إصدار الواجهة في تقارير الأخطاء.

## 3.4 تجربة الهاتف

- مراجعة كل صفحة ذات أولوية على أحجام الهاتف.
- منع الاعتماد على Hover.
- جعل الحقول والأزرار مناسبة للمس.
- تحويل الجداول غير العملية إلى بطاقات أو عرض مختصر.
- منع اختفاء الحقول خلف لوحة المفاتيح.
- حفظ موضع المستخدم والمسودة عند الرجوع.
- Skeleton loading وحالات فارغة وأخطاء واضحة.

## بوابة القبول

- التطبيق قابل للتثبيت ويعمل في وضع مستقل.
- Offline shell يعمل دون كشف بيانات مستخدم سابق.
- التحديث لا يفقد نموذجًا مفتوحًا.
- اختبارات Lighthouse ليست المعيار الوحيد؛ تنجح الرحلات الفعلية على أجهزة أندرويد ضعيفة ومتوسطة.

---

# المرحلة 4: التخزين المحلي وOffline

## 4.1 هل نستخدم قاعدة داخلية؟

نعم، لكن حسب المنصة:

- **PWA:** تستخدم IndexedDB للبيانات المنظمة والمسودات. لا يستخدم `localStorage` للبيانات الكبيرة أو طوابير المزامنة.
- **Flutter:** تستخدم SQLite عبر طبقة Repository، أو محرك محلي موثوق مبني عليها، مع تشفير القيم الحساسة عند الحاجة.

القاعدة المحلية ليست نسخة مستقلة من قاعدة Laravel، بل Cache ومسودات وطابور عمليات محدود.

## 4.2 تصنيف البيانات

### يسمح محليًا

- إعدادات العرض.
- آخر بيانات مرجعية يحتاجها المستخدم.
- مسودات الجرد غير المعتمدة.
- مسودة نقل لم ترسل.
- فلاتر البحث.
- آخر مزامنة.
- Outbox metadata دون أسرار.

### يمنع اعتماده Offline

- بيع نهائي.
- تحصيل أو مصروف مالي.
- إغلاق شفت.
- قبول أو رفض نقل.
- اعتماد جرد.
- تعديل مباشر للمخزون.
- حذف أو اعتماد طلب توريد.

## 4.3 نموذج المزامنة

كل مسودة أو أمر قابل للإرسال يحمل:

- `local_id`.
- `idempotency_key`.
- نوع العملية.
- وقت الإنشاء المحلي.
- إصدار البيانات المصدرية.
- الحالة: draft، queued، sending، synced، conflict، failed.
- عدد المحاولات.
- آخر خطأ مفهوم للمستخدم.

## 4.4 التعارض

لا نطبق Last Write Wins على المخزون أو المال. عند التعارض:

1. يوقف الاعتماد.
2. يجلب الخادم أحدث نسخة.
3. يوضح للمستخدم ما تغير.
4. يطلب مراجعة وإرسالًا جديدًا.

## 4.5 تنظيف البيانات

- حذف Cache المستخدم عند تسجيل الخروج.
- فصل قاعدة كل حساب/متجر منطقيًا.
- مدة صلاحية لكل نوع بيانات.
- حد أعلى للحجم.
- عدم نسخ التقارير المالية كاملة دون ضرورة.

## بوابة القبول

- انقطاع الشبكة لا يفقد مسودة مسموحة.
- عودة الشبكة لا تكرر العملية.
- تبديل المستخدم لا يعرض بيانات المستخدم السابق.
- التعارض يظهر للمستخدم ولا يعدّل المخزون بصمت.

---

# المرحلة 5: الأداء والضغط وقاعدة البيانات

## 5.1 الرصد قبل التحسين

تقاس المؤشرات التالية:

- p50 وp95 وp99 لزمن الاستجابة.
- معدل أخطاء 4xx و5xx.
- الاستعلامات البطيئة.
- عدد الاستعلامات لكل Endpoint.
- Queue lag.
- CPU والذاكرة والقرص.
- الاتصالات بقاعدة البيانات.
- Cache hit rate.
- حجم الاستجابات.
- زمن البحث.

## 5.2 تحسين Laravel

- إزالة N+1 باستخدام eager loading محدد الأعمدة.
- Pagination لكل قائمة كبيرة.
- نقل PDF والإشعارات والتقارير الثقيلة إلى Queue.
- Cache للبيانات المرجعية الآمنة فقط.
- عدم Cache الأرصدة الحساسة دون سياسة invalidation واضحة.
- ضغط الاستجابات والأصول.
- Timeouts وحدود واضحة للخدمات الخارجية.
- Health checks للويب والـQueue وقاعدة البيانات والتخزين.

## 5.3 تحسين قاعدة البيانات

- فهارس مركبة حسب `store_id` والحالة والتاريخ المستخدمة فعليًا.
- مراجعة `EXPLAIN` للاستعلامات البطيئة.
- منع `SELECT *` في API.
- أرشفة أو Partitioning فقط بعد إثبات الحاجة.
- Connection pooling إذا أثبت الحمل الحاجة.
- نسخ احتياطي مجرب بالاسترجاع، لا مجرد ملف Backup.

## 5.4 اختبارات الحمل

سيناريوهات واقعية:

- دخول عدد كبير من المحاسبين بداية اليوم.
- بحث منتجات متزامن.
- مبيعات متزامنة على المنتج نفسه.
- وصول إشعارات جماعية.
- إنشاء تقارير شهرية.
- نقل وجرد متزامنان.

## بوابة القبول

توضع SLOs رقمية بعد قياس Staging، ثم لا يطلق الإصدار إذا تجاوز p95 أو معدل الخطأ الحدود المتفق عليها.

---

# المرحلة 6: نظام التصميم ومركز التحكم

## 6.1 الهدف

إدارة الهوية من مصدر واحد للويب وPWA وFlutter دون السماح بإفساد القراءة أو الحالات التشغيلية.

## 6.2 Design Tokens

تعرف رموز موحدة مثل:

- Brand.
- Background/Surface.
- Text/Muted.
- Border.
- Success/Warning/Danger/Info.
- المسافات.
- نصف القطر.
- أحجام الخط.
- الظلال.
- كثافة الواجهة.

يستهلك الويب الرموز كـCSS variables، ويستهلك Flutter نسخة JSON/Dart مولدة من المصدر نفسه.

## 6.3 مركز التحكم

ينقسم إلى مستويين:

### إعدادات آمنة للمالك أو الإدارة

- شعار المؤسسة.
- الاسم الظاهر.
- الوضع المظلم/الفاتح المسموح.
- لون علامة ضمن لوحة ألوان معتمدة.
- كثافة العرض.
- معاينة قبل النشر.

### إعدادات محمية للمطور

- ألوان الحالات.
- نسب التباين.
- Typography.
- أحجام عناصر اللمس.
- قواعد PDF.
- Tokens البنيوية.

لا يسمح للمستخدم بتغيير لون الخطر أو النجاح أو اختيار لون يجعل النص غير مقروء.

## 6.4 النشر والإصدار

- كل Theme له إصدار.
- Draft ثم Preview ثم Publish.
- إمكانية الرجوع للإصدار السابق.
- Audit log لمن عدّل ونشر.
- Cache busting بعد النشر.
- Endpoint مثل `/api/v1/app-config` يعيد الإعدادات المسموحة ونسخة الهوية.

## بوابة القبول

- نفس الحالات والأسماء والألوان المعتمدة تظهر في الويب وFlutter.
- تغيير الهوية لا يحتاج تعديل عشرات الصفحات.
- يمكن الرجوع عن Theme خاطئ فورًا.

---

# المرحلة 7: الإشعارات والتجربة الحية

## الأعمال

- Event catalog موحد لكل إشعار.
- Inbox داخلي هو المصدر، وPush قناة توصيل إضافية.
- Deep links موحدة بين PWA وFlutter.
- عدادات غير مقروءة صحيحة.
- تفضيلات إشعار حسب النوع دون تعطيل الإشعارات الحرجة.
- Queue وإعادة محاولة وDead-letter monitoring.
- Realtime للأحداث التي تستحق، لا Polling كثيف لكل الصفحات.
- إخفاء التفاصيل الحساسة من شاشة قفل الهاتف.

## بوابة القبول

- لا يصل إشعار لمتجر غير مصرح به.
- الضغط يفتح السجل الصحيح بعد التحقق من الصلاحية.
- فشل Push لا يفقد الإشعار الداخلي.

---

# المرحلة 8: Flutter MVP

## النطاق الأول

1. تسجيل الدخول وإدارة الجهاز.
2. اختيار المتجر حسب الصلاحية.
3. لوحة اليوم.
4. مركز الإشعارات.
5. البحث عن المنتجات والباركود.
6. النقل المخزني.
7. الجرد والمسودات.
8. حالة الشفت.
9. الملف الشخصي والخروج.

## البنية المقترحة

- Feature-first modules.
- Repository يفصل API عن قاعدة SQLite.
- State management موحد يختاره الفريق ويوثق.
- Secure Storage للتوكنات.
- SQLite للمسودات والـCache المنظم.
- Outbox محدود للعمليات المسموحة.
- Crash reporting وperformance tracing.
- بيئات Build منفصلة.

## ما يؤجل

- التقارير الكبيرة.
- إدارة المستخدمين.
- إعدادات النظام الكاملة.
- Offline للعمليات المالية.
- الطباعة المتخصصة حتى تتحدد الأجهزة.

## بوابة القبول

- Pilot على متجر واحد وحسابات محددة.
- مقارنة نتائج العمليات بين الويب وFlutter.
- لا توجد فروقات مخزون أو صلاحيات.
- نجاح خطة إلغاء الإصدار أو إجبار التحديث.

---

# المرحلة 9: الإطلاق التدريجي

## الترتيب

1. بيئة Staging ببيانات مصطنعة.
2. فريق داخلي.
3. متجر تجريبي واحد.
4. مجموعة محاسبين محدودة.
5. 10% من المستخدمين.
6. 50% بعد استقرار المؤشرات.
7. إطلاق عام.

## أدوات الأمان

- Feature flags.
- Kill switch للمزامنة.
- إجبار تحديث النسخة.
- Rollback للخادم والتطبيق.
- مراقبة لحظية للأخطاء.
- قناة دعم داخلية.

---

# ترتيب الأولويات التنفيذي

## أولوية P0 — قبل PWA

- تدقيق الصلاحيات وعزل المتاجر.
- تدقيق المخزون والتزامن.
- إصلاح دورة الإشعارات.
- قياس البحث والاستعلامات.
- نسخ احتياطي واسترجاع مجرب.
- اختبارات Feature للعمليات الحساسة.

## أولوية P1 — PWA قابلة للاستخدام

- Manifest والأيقونات.
- Service Worker آمن.
- Offline shell.
- تحديث التطبيق.
- تجربة الهاتف.
- تسجيل الأجهزة وDeep links.

## أولوية P2 — Backend مشترك

- API v1.
- مصادقة Tokens.
- استجابات وأخطاء موحدة.
- Idempotency.
- App config وDesign tokens.
- Observability وRate limits.

## أولوية P3 — Offline منضبط

- IndexedDB للمسودات.
- Outbox.
- شاشة المزامنة والتعارض.
- تنظيف البيانات عند الخروج.

## أولوية P4 — Flutter

- MVP محدود.
- SQLite وRepository.
- Push وDeep links.
- Pilot ثم إطلاق تدريجي.

---

# لوحة متابعة الخارطة

لكل مهمة تسجل الحقول التالية:

| الحقل | الوصف |
| --- | --- |
| المعرف | مثل `PWA-03` أو `API-12` |
| المسار | PWA، API، Flutter، Security، Performance، Design |
| الهدف | النتيجة التشغيلية، لا اسم الملف فقط |
| المالك | المسؤول عن القرار والتنفيذ |
| الحالة | Backlog، Ready، In Progress، Review، Done، Blocked |
| الاعتماديات | المهام التي يجب أن تسبقها |
| المخاطر | بيانات، صلاحيات، تزامن، أداء |
| الاختبارات | Unit، Feature، Contract، E2E، Load |
| Feature Flag | اسم العلم أو غير مطلوب |
| خطة الرجوع | كيفية تعطيلها دون فقد البيانات |
| معيار القبول | شرط قابل للقياس |

## قواعد إدارة العمل

- لا توجد أكثر من مرحلة بنيوية كبيرة قيد التنفيذ بلا مالك واضح.
- لا تنتقل المهمة إلى Done لمجرد اكتمال الكود؛ يجب اكتمال الاختبار والتوثيق والمراقبة.
- كل Sprint يحتوي أعمال منتج وأعمال دين تقني وأعمال مراقبة.
- تسجل القرارات الكبيرة في ADR ولا تعاد مناقشتها دون معلومة جديدة.
- تراجع الخارطة كل أسبوعين، والمخاطر شهريًا.

---

# مؤشرات النجاح

## المنتج

- نسبة تثبيت PWA.
- المستخدمون النشطون يوميًا.
- زمن إكمال الجرد والنقل.
- نسبة المسودات المستعادة.
- انخفاض العمليات المتروكة.

## الجودة

- Crash-free sessions.
- معدل فشل API.
- تكرار العمليات بسبب الشبكة.
- تعارضات المزامنة.
- أخطاء الصلاحيات.

## الأداء

- p95 لكل Endpoint مهم.
- زمن البحث.
- حجم الحزمة الأولية.
- زمن فتح PWA بعد الزيارة الأولى.
- Queue lag.

## التشغيل

- نجاح النسخ الاحتياطي والاسترجاع.
- زمن اكتشاف العطل.
- زمن الاستعادة.
- نسبة تسليم الإشعارات.
- عدد الإصدارات التي احتاجت Rollback.

---

# أول حزمة عمل مقترحة

لا تبدأ الحزمة الأولى بكتابة Service Worker مباشرة. تبدأ بالترتيب التالي:

1. جرد الصفحات والرحلات والأدوار.
2. تصنيف العمليات Offline/Online.
3. تدقيق الإشعارات والأجهزة.
4. قياس البحث والاستعلامات الأكثر استخدامًا.
5. إضافة مراقبة أساسية وRequest IDs.
6. تصميم عقد `/api/v1/me` و`/api/v1/app-config` دون ربط Flutter بعد.
7. إعداد Manifest والأيقونات وOffline shell تجريبي.
8. اختبار PWA على أجهزة حقيقية.
9. اختيار رحلة واحدة للمسودة المحلية، ويفضل الجرد غير المعتمد.
10. مراجعة النتائج قبل توسيع النطاق.

# قرار التخزين المحلي النهائي

- نعم لقاعدة داخلية في PWA وFlutter.
- لا لنسخة محلية مستقلة من النظام المالي أو المخزون.
- PWA تستخدم IndexedDB.
- Flutter تستخدم SQLite.
- الاعتماد النهائي للعمليات الحساسة يبقى Online ومن Laravel.
- كل إرسال قابل للتكرار يستخدم Idempotency Key.
- كل تعارض حساس يتطلب مراجعة، ولا يحل تلقائيًا.

# تعريف النجاح النهائي

تنجح الخارطة عندما يصبح لدينا:

- ويب وPWA وFlutter يستخدمون Backend واحدًا.
- منطق العمل غير مكرر.
- تجربة هاتف سريعة وقابلة للتثبيت.
- مسودات آمنة عند انقطاع الشبكة.
- إشعارات موثوقة بروابط عميقة.
- بحث سريع قابل للقياس.
- قاعدة بيانات مراقبة ومفهرسة وفق الاستخدام.
- مركز هوية موحد بإصدارات ورجوع آمن.
- إطلاق تدريجي يمكن إيقافه دون خسارة بيانات.
