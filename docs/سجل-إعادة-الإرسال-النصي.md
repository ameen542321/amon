# سجل إعادة الإرسال النصي

**المعرف:** `AMON-TEXT-ONLY-RESEND-2026-09-25`

أعيد تجهيز الطلب من الالتزام الأساسي `9257f4b` في التزام واحد نهائي. فُحص الفرق
الكامل قبل إعادة الإرسال، ولا يحتوي على أي ملف ثنائي أو أي إضافة ثم حذف لأصول
PWA داخل تاريخ هذا الطلب.

الحالة النهائية المقصودة:

- تبقى تحسينات الإشعارات وQueue والحماية والنقل المخزني والجرد والتقارير.
- لا ترسل ملفات Manifest أو Service Worker أو Offline shell أو أيقونات PWA.
- لا يتضمن هذا السجل أي تعديل على قاعدة البيانات أو البيانات الحقيقية.
- جميع الملفات المضافة في إعادة الإرسال الحالية ملفات مصدر أو مستندات نصية.

أمر التدقيق المستخدم:

```bash
git diff --numstat 9257f4b..HEAD
```

أي سطر يظهر بالقيمتين `- -` يعني وجود ملف ثنائي ويمنع اعتماد إعادة الإرسال.
وقت إنشاء هذا السجل لم يظهر أي سطر من هذا النوع.
